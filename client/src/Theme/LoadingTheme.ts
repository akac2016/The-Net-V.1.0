import { LoadScreenTheme } from 'styled-components'

const loadingTheme : LoadScreenTheme = {
    colors: {
        background: {
            main: '#011638',
        },
        text: {
            primary: 'white',
        }
    }
}

export default loadingTheme